{
  "name": "warrant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Bonded execution for onchain agents — KeeperHub Agents Onchain hackathon",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "test:contracts": "cd contracts && forge test -vv",
    "fixtures": "vite-node packages/core/scripts/gen-fixtures.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@11.17.0"
}
